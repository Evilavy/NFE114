<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Connexion - AlloParents{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    animation: {
                        'fade-in': 'fadeIn 0.8s ease-out',
                        'slide-up': 'slideUp 0.6s ease-out',
                        'pulse-glow': 'pulseGlow 3s ease-in-out infinite',
                        'float': 'float 6s ease-in-out infinite',
                        'shimmer': 'shimmer 2s linear infinite',
                    },
                    keyframes: {
                        fadeIn: {
                            '0%': { opacity: '0', transform: 'scale(0.95)' },
                            '100%': { opacity: '1', transform: 'scale(1)' }
                        },
                        slideUp: {
                            '0%': { transform: 'translateY(30px)', opacity: '0' },
                            '100%': { transform: 'translateY(0)', opacity: '1' }
                        },
                        pulseGlow: {
                            '0%, 100%': { boxShadow: '0 0 40px rgba(255, 255, 255, 0.1)' },
                            '50%': { boxShadow: '0 0 80px rgba(255, 255, 255, 0.2)' }
                        },
                        float: {
                            '0%, 100%': { transform: 'translateY(0px)' },
                            '50%': { transform: 'translateY(-10px)' }
                        },
                        shimmer: {
                            '0%': { backgroundPosition: '-200% center' },
                            '100%': { backgroundPosition: '200% center' }
                        }
                    }
                }
            }
        }
    </script>
    <style>
        .gradient-bg {
            background: linear-gradient(135deg,
                #ffffff 0%,
                #f8fafc 25%,
                #f1f5f9 50%,
                #e2e8f0 75%,
                #cbd5e1 100%
            );
        }
        
        .glass-morphism {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(0, 0, 0, 0.08);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.08), 0 4px 16px rgba(0, 0, 0, 0.04);
        }
        
        .input-glass {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(0, 0, 0, 0.1);
        }
        
        .input-glass:focus {
            background: rgba(255, 255, 255, 1);
            border-color: rgba(59, 130, 246, 0.5);
            box-shadow: 0 0 30px rgba(59, 130, 246, 0.1);
        }
        
        .btn-gradient {
            background: linear-gradient(45deg, #3b82f6, #2563eb, #1d4ed8);
            background-size: 200% 200%;
            animation: shimmer 3s ease-in-out infinite;
        }
        
        .btn-gradient:hover {
            background: linear-gradient(45deg, #2563eb, #1d4ed8, #1e40af);
        }
        
        .floating-orbs::before,
        .floating-orbs::after {
            content: '';
            position: absolute;
            border-radius: 50%;
            background: radial-gradient(circle, rgba(59, 130, 246, 0.1) 0%, transparent 70%);
        }
        
        .floating-orbs::before {
            width: 300px;
            height: 300px;
            top: -150px;
            right: -150px;
            animation: float 8s ease-in-out infinite;
        }
        
        .floating-orbs::after {
            width: 200px;
            height: 200px;
            bottom: -100px;
            left: -100px;
            animation: float 6s ease-in-out infinite reverse;
        }
        
        .text-shadow {
            text-shadow: 0 2px 20px rgba(0,0,0,0.1);
        }
    </style>
</head>

<body class="gradient-bg min-h-screen overflow-hidden">
    <!-- Animated Background -->
    <div class="floating-orbs absolute inset-0 pointer-events-none"></div>
    
    <!-- Noise Texture Overlay -->
    <div class="absolute inset-0 opacity-[0.02]" style="background-image: url('data:image/svg+xml,<svg viewBox="0 0 256 256" xmlns="http://www.w3.org/2000/svg"><defs><filter id="noiseFilter"><feTurbulence type="fractalNoise" baseFrequency="0.85" numOctaves="4" stitchTiles="stitch"/></filter></defs><rect width="100%" height="100%" filter="url(%23noiseFilter)" opacity="0.4"/></svg>');"></div>

    <!-- Main Container -->
    <div class="relative min-h-screen flex items-center justify-center p-4 sm:p-6">
        <div class="w-full max-w-sm sm:max-w-md">
            
            <!-- Logo/Header Section -->
            <div class="text-center mb-8 sm:mb-10">
                <div class="inline-flex items-center justify-center w-16 h-16 sm:w-20 sm:h-20 rounded-full glass-morphism mb-6 group">
                    <svg class="w-8 h-8 sm:w-10 sm:h-10 text-blue-600 group-hover:scale-110 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
                    </svg>
                </div>
                <h1 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-gray-800 mb-3 text-shadow">AlloParents</h1>
                <p class="text-gray-600 text-base sm:text-lg opacity-90">Connectez-vous à votre espace</p>
            </div>

            <!-- Login Card -->
            <div class="glass-morphism rounded-3xl p-6 sm:p-8 shadow-2xl relative overflow-hidden">
                
                <!-- Subtle Inner Glow -->
                <div class="absolute inset-0 rounded-3xl bg-gradient-to-br from-white/[0.02] to-transparent pointer-events-none"></div>
                
                <div class="relative">
                    <!-- Lockout Warning -->
                    {% if is_locked %}
                        <div class="mb-6 p-4 rounded-2xl bg-orange-500/10 border border-orange-500/20 backdrop-blur-sm">
                            <div class="flex items-start">
                                <svg class="w-5 h-5 text-orange-600 mr-3 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
                                </svg>
                                <div>
                                    <p class="text-orange-700 text-sm font-medium">Accès temporairement bloqué</p>
                                    <p class="text-orange-600 text-xs mt-1 opacity-90">
                                        Trop de tentatives de connexion échouées. Réessayez dans {{ formatted_remaining_time }}.
                                    </p>
                                </div>
                            </div>
                        </div>
                    {% elseif remaining_attempts < rate_limiter_config.max_attempts and remaining_attempts > 0 %}
                        <div class="mb-6 p-4 rounded-2xl bg-yellow-500/10 border border-yellow-500/20 backdrop-blur-sm">
                            <div class="flex items-start">
                                <svg class="w-5 h-5 text-yellow-600 mr-3 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L3.732 16.5c-.77.833.192 2.5 1.732 2.5z"/>
                                </svg>
                                <div>
                                    <p class="text-yellow-700 text-sm font-medium">Attention</p>
                                    <p class="text-yellow-600 text-xs mt-1 opacity-90">
                                        Il vous reste {{ remaining_attempts }} tentative{% if remaining_attempts > 1 %}s{% endif %} avant le blocage temporaire.
                                    </p>
                                </div>
                            </div>
                        </div>
                    {% endif %}

                    <!-- Error Messages -->
                    {% if error %}
                        <div class="mb-6 p-4 rounded-2xl bg-red-500/10 border border-red-500/20 backdrop-blur-sm">
                            <div class="flex items-start">
                                <svg class="w-5 h-5 text-red-600 mr-3 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                                </svg>
                                <div>
                                    <p class="text-red-700 text-sm font-medium">Erreur de connexion</p>
                                    <p class="text-red-600 text-xs mt-1 opacity-90">
                                        {% if error.messageKey == 'Bad credentials.' %}
                                            Email ou mot de passe incorrect.
                                        {% else %}
                                            {{ error.messageKey|trans(error.messageData, 'security') }}
                                        {% endif %}
                                    </p>
                                </div>
                            </div>
                        </div>
                    {% endif %}

                    <!-- Already Logged In -->
                    {% if app.user %}
                        <div class="mb-6 p-4 rounded-2xl bg-emerald-500/10 border border-emerald-500/20 backdrop-blur-sm">
                            <div class="flex items-center justify-between">
                                <div class="flex items-center">
                                    <svg class="w-5 h-5 text-emerald-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                                    </svg>
                                    <span class="text-emerald-700 text-sm font-medium">{{ app.user.userIdentifier }}</span>
                                </div>
                                <a href="{{ path('app_logout') }}" class="text-emerald-600 hover:text-emerald-800 text-xs font-medium underline decoration-1 underline-offset-2 hover:decoration-2 transition-all">
                                    Déconnexion
                                </a>
                            </div>
                        </div>
                    {% endif %}

                    <!-- Login Form -->
                    <form method="post" class="space-y-5"{% if is_locked %} style="pointer-events: none; opacity: 0.6;"{% endif %}>
                        
                        <!-- Email Field -->
                        <div class="space-y-2">
                            <label for="inputEmail" class="block text-sm font-medium text-gray-700">
                                Adresse email
                            </label>
                            <div class="relative group">
                                <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                                    <svg class="w-5 h-5 text-gray-500 group-focus-within:text-blue-600 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 12a4 4 0 10-8 0 4 4 0 008 0zm0 0v1.5a2.5 2.5 0 005 0V12a9 9 0 10-9 9m4.5-1.206a8.959 8.959 0 01-4.5 1.207"/>
                                    </svg>
                                </div>
                                <input 
                                    type="email" 
                                    value="{{ last_username }}" 
                                    name="_username" 
                                    id="inputEmail" 
                                    autocomplete="email" 
                                    required 
                                    autofocus
                                    class="w-full pl-12 pr-4 py-4 input-glass rounded-2xl text-gray-800 placeholder-gray-500 focus:outline-none transition-all duration-300 text-sm sm:text-base"
                                    placeholder="votre@email.com"
                                >
                            </div>
                        </div>

                        <!-- Password Field -->
                        <div class="space-y-2">
                            <label for="inputPassword" class="block text-sm font-medium text-gray-700">
                                Mot de passe
                            </label>
                            <div class="relative group">
                                <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                                    <svg class="w-5 h-5 text-gray-500 group-focus-within:text-blue-600 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
                                    </svg>
                                </div>
                                <input 
                                    type="password" 
                                    name="_password" 
                                    id="inputPassword" 
                                    autocomplete="current-password" 
                                    required
                                    class="w-full pl-12 pr-4 py-4 input-glass rounded-2xl text-gray-800 placeholder-gray-500 focus:outline-none transition-all duration-300 text-sm sm:text-base"
                                    placeholder="••••••••••"
                                >
                            </div>
                        </div>

                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                        <!-- Submit Button -->
                        <button 
                            type="submit" 
                            {% if is_locked %}disabled{% endif %}
                            class="w-full py-4 px-6 btn-gradient text-white font-semibold rounded-2xl transition-all duration-300 transform hover:scale-[1.02] focus:outline-none focus:ring-2 focus:ring-blue-500/20 shadow-lg hover:shadow-xl relative overflow-hidden group {% if is_locked %}opacity-50 cursor-not-allowed{% endif %}"
                        >
                            <span class="relative flex items-center justify-center text-sm sm:text-base">
                                <svg class="w-5 h-5 mr-2 group-hover:rotate-12 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1"/>
                                </svg>
                                Se connecter
                            </span>
                        </button>
                    </form>

                    <!-- Divider -->
                    <div class="relative my-8">
                        <div class="absolute inset-0 flex items-center">
                            <div class="w-full border-t border-gray-200"></div>
                        </div>
                        <div class="relative flex justify-center text-xs">
                            <span class="px-4 bg-transparent text-gray-500">ou</span>
                        </div>
                    </div>

                    <!-- Register Link -->
                    <div class="text-center space-y-4">
                        <p class="text-gray-600 text-sm">Vous n'avez pas encore de compte ?</p>
                        <a href="{{ path('app_register') }}" class="inline-flex items-center justify-center w-full py-4 px-6 border border-gray-300 hover:border-blue-500 rounded-2xl text-gray-700 font-medium hover:bg-blue-50 transition-all duration-300 transform hover:scale-[1.02] focus:outline-none focus:ring-2 focus:ring-blue-500/20 text-sm sm:text-base">
                            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"/>
                            </svg>
                            Créer un compte
                        </a>
                    </div>
                </div>
            </div>

            <!-- Footer -->
            <div class="text-center mt-8 space-y-2 animate-fade-in">
                <p class="text-gray-500 text-xs opacity-70">
                    &copy; 2025 AlloParents. Tous droits réservés.
                </p>
                <p class="text-gray-600 text-xs opacity-50">
                    Transport scolaire simplifié
                </p>
            </div>
        </div>
    </div>

    <!-- Enhanced JavaScript -->
    <script>
        // Enhanced input interactions
        document.querySelectorAll('input').forEach((input, index) => {
            // Focus effects
            input.addEventListener('focus', function() {
                this.parentElement.querySelector('svg')?.classList.add('text-blue-600');
            });
            
            input.addEventListener('blur', function() {
                this.parentElement.querySelector('svg')?.classList.remove('text-blue-600');
            });
        });

        // Enhanced button interaction
        const submitBtn = document.querySelector('button[type="submit"]');
        // Keep the button functional without animations

        // Auto-focus on email field immediately
        document.getElementById('inputEmail')?.focus();
    </script>
</body>
</html>