<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Détails utilisateur - AlloParents Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    animation: {
                        'fade-in': 'fadeIn 0.6s ease-out',
                        'slide-in-left': 'slideInLeft 0.8s ease-out',
                        'slide-in-right': 'slideInRight 0.8s ease-out',
                        'pulse-glow': 'pulseGlow 3s ease-in-out infinite',
                        'scale-in': 'scaleIn 0.4s ease-out',
                        'float': 'float 6s ease-in-out infinite',
                        'counter-up': 'counterUp 1s ease-out',
                        'bounce-subtle': 'bounceSubtle 2s ease-in-out infinite',
                    },
                    keyframes: {
                        fadeIn: {
                            '0%': { opacity: '0', transform: 'scale(0.95)' },
                            '100%': { opacity: '1', transform: 'scale(1)' }
                        },
                        slideInLeft: {
                            '0%': { transform: 'translateX(-30px)', opacity: '0' },
                            '100%': { transform: 'translateX(0)', opacity: '1' }
                        },
                        slideInRight: {
                            '0%': { transform: 'translateX(30px)', opacity: '0' },
                            '100%': { transform: 'translateX(0)', opacity: '1' }
                        },
                        pulseGlow: {
                            '0%, 100%': { boxShadow: '0 0 20px rgba(255, 255, 255, 0.1)' },
                            '50%': { boxShadow: '0 0 40px rgba(255, 255, 255, 0.2)' }
                        },
                        scaleIn: {
                            '0%': { transform: 'scale(0.9)', opacity: '0' },
                            '100%': { transform: 'scale(1)', opacity: '1' }
                        },
                        float: {
                            '0%, 100%': { transform: 'translateY(0px)' },
                            '50%': { transform: 'translateY(-10px)' }
                        },
                        counterUp: {
                            '0%': { transform: 'scale(0.8)', opacity: '0' },
                            '100%': { transform: 'scale(1)', opacity: '1' }
                        },
                        bounceSubtle: {
                            '0%, 100%': { transform: 'translateY(0)' },
                            '50%': { transform: 'translateY(-5px)' }
                        }
                    }
                }
            }
        }
    </script>
    <style>
        .gradient-bg {
            background: linear-gradient(135deg, 
                #000000 0%, 
                #1a1a1a 20%, 
                #2d2d2d 40%, 
                #404040 60%, 
                #737373 80%, 
                #ffffff 100%
            );
        }
        
        .glass-card {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .glass-input {
            background: rgba(255, 255, 255, 0.08);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.15);
        }
        
        .btn-primary {
            background: linear-gradient(45deg, #3b82f6, #2563eb, #1d4ed8);
            transition: all 0.3s ease;
        }
        
        .btn-primary:hover {
            background: linear-gradient(45deg, #2563eb, #1d4ed8, #1e40af);
            transform: translateY(-2px);
        }
        
        .btn-success {
            background: linear-gradient(45deg, #10b981, #059669, #047857);
            transition: all 0.3s ease;
        }
        
        .btn-success:hover {
            background: linear-gradient(45deg, #059669, #047857, #065f46);
            transform: translateY(-2px);
        }
        
        .btn-danger {
            background: linear-gradient(45deg, #ef4444, #dc2626, #b91c1c);
            transition: all 0.3s ease;
        }
        
        .btn-danger:hover {
            background: linear-gradient(45deg, #dc2626, #b91c1c, #991b1b);
            transform: translateY(-2px);
        }

        .btn-secondary {
            background: linear-gradient(45deg, #6b7280, #4b5563, #374151);
            transition: all 0.3s ease;
        }
        
        .btn-secondary:hover {
            background: linear-gradient(45deg, #4b5563, #374151, #1f2937);
            transform: translateY(-2px);
        }

        .floating-orbs::before,
        .floating-orbs::after {
            content: '';
            position: absolute;
            border-radius: 50%;
            background: radial-gradient(circle, rgba(255,255,255,0.08) 0%, transparent 70%);
            pointer-events: none;
        }
        
        .floating-orbs::before {
            width: 400px;
            height: 400px;
            top: -200px;
            right: -200px;
            animation: float 15s ease-in-out infinite;
        }
        
        .floating-orbs::after {
            width: 300px;
            height: 300px;
            bottom: -150px;
            left: -150px;
            animation: float 10s ease-in-out infinite reverse;
        }

        @keyframes float {
            0%, 100% { transform: rotate(0deg) translate(-20px) rotate(0deg); }
            50% { transform: rotate(180deg) translate(-20px) rotate(-180deg); }
        }

        .text-shadow {
            text-shadow: 0 2px 20px rgba(0,0,0,0.5);
        }

        .status-approved {
            background: linear-gradient(45deg, #10b981, #059669);
            animation: pulse-glow 2s ease-in-out infinite;
        }

        .status-pending {
            background: linear-gradient(45deg, #f59e0b, #d97706);
            animation: pulse-glow 2s ease-in-out infinite;
        }

        .status-verified {
            background: linear-gradient(45deg, #3b82f6, #1d4ed8);
        }

        .status-unverified {
            background: linear-gradient(45deg, #6b7280, #4b5563);
        }

        .role-badge-parent {
            background: linear-gradient(45deg, #3b82f6, #1d4ed8);
        }

        .role-badge-grandparent {
            background: linear-gradient(45deg, #8b5cf6, #7c3aed);
        }

        .role-badge-other {
            background: linear-gradient(45deg, #6b7280, #4b5563);
        }

        .points-badge {
            background: linear-gradient(45deg, #f59e0b, #d97706);
            animation: bounce-subtle 2s ease-in-out infinite;
        }

        .profile-avatar {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.4);
        }
    </style>
</head>

<body class="gradient-bg min-h-screen">
    <!-- Background Effects -->
    <div class="floating-orbs fixed inset-0 pointer-events-none"></div>
    
    <!-- Top Navigation -->
    <nav class="fixed top-0 left-0 right-0 z-50 glass-card border-b border-white/10">
        <div class="px-4 py-3">
            <div class="flex items-center justify-between">
                <div class="flex items-center space-x-3">
                    <a href="{{ path('admin_pending_users') }}" class="w-8 h-8 rounded-full bg-gradient-to-r from-gray-800 to-gray-600 flex items-center justify-center">
                        <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
                        </svg>
                    </a>
                    <h1 class="text-white font-bold text-lg text-shadow">Détails utilisateur</h1>
                </div>
                <div class="flex items-center space-x-3">
                    <div class="w-8 h-8 bg-gradient-to-r from-indigo-600 to-indigo-500 rounded-full flex items-center justify-center">
                        <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                        </svg>
                    </div>
                    <a href="{{ path('admin_dashboard') }}" class="w-8 h-8 bg-gradient-to-r from-purple-600 to-purple-500 rounded-full flex items-center justify-center hover:from-purple-700 hover:to-purple-600 transition-all duration-300" title="Administration">
                        <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/>
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                        </svg>
                    </a>
                    <a href="{{ path('app_logout') }}" class="w-8 h-8 bg-gradient-to-r from-red-600 to-red-500 rounded-full flex items-center justify-center hover:from-red-700 hover:to-red-600 transition-all duration-300" title="Déconnexion">
                        <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
                        </svg>
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="pt-20 px-4 pb-24">
        
        <!-- User Profile Header -->
        <div class="mb-8 glass-card rounded-3xl p-8 animate-fade-in">
            <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-8">
                <div class="flex flex-col sm:flex-row items-center sm:items-start space-y-6 sm:space-y-0 sm:space-x-6">
                    <!-- Avatar -->
                    <div class="profile-avatar w-24 h-24 rounded-full flex items-center justify-center">
                        <span class="text-white font-bold text-2xl">{{ user.prenom|slice(0, 1) }}{{ user.nom|slice(0, 1) }}</span>
                    </div>
                    
                    <!-- User Info -->
                    <div class="text-center sm:text-left">
                        <h1 class="text-3xl font-bold text-white mb-2">{{ user.prenom }} {{ user.nom }}</h1>
                        <p class="text-xl text-gray-300 mb-4">{{ user.email }}</p>
                        
                        <!-- Status Badges -->
                        <div class="flex flex-wrap justify-center sm:justify-start gap-3">
                            {% if user.isApprovedByAdmin %}
                                <span class="status-approved text-white text-sm font-bold px-3 py-1 rounded-full">
                                    ✓ Approuvé
                                </span>
                            {% else %}
                                <span class="status-pending text-white text-sm font-bold px-3 py-1 rounded-full">
                                    ⏳ En attente
                                </span>
                            {% endif %}
                            
                            {% if user.isVerified %}
                                <span class="status-verified text-white text-sm font-bold px-3 py-1 rounded-full">
                                    📧 E-mail vérifié
                                </span>
                            {% else %}
                                <span class="status-unverified text-white text-sm font-bold px-3 py-1 rounded-full">
                                    📧 E-mail non vérifié
                                </span>
                            {% endif %}
                            
                            {% if user.role == 'parent' %}
                                <span class="role-badge-parent text-white text-sm font-bold px-3 py-1 rounded-full">
                                    👨‍👩‍👧‍👦 Parent
                                </span>
                            {% elseif user.role == 'grand_parent' %}
                                <span class="role-badge-grandparent text-white text-sm font-bold px-3 py-1 rounded-full">
                                    👴👵 Grand-parent
                                </span>
                            {% elseif user.role == 'autre' %}
                                <span class="role-badge-other text-white text-sm font-bold px-3 py-1 rounded-full">
                                    👤 {{ user.roleAutre ?: 'Autre' }}
                                </span>
                            {% endif %}
                            
                            <span class="points-badge text-white text-sm font-bold px-3 py-1 rounded-full">
                                🏆 {{ user.points }} points
                            </span>
            </div>
        </div>
    </div>

                <!-- Quick Actions -->
                <div class="flex flex-col gap-3">
                    {% if not user.isApprovedByAdmin %}
                        <form method="POST" action="{{ path('admin_approve_user', {'id': user.id}) }}" class="inline">
                            <button type="submit" 
                                    class="btn-success text-white font-medium py-3 px-6 rounded-2xl transition-all duration-300 text-center transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-white/20 shadow-lg hover:shadow-xl w-full"
                                    onclick="return confirm('Êtes-vous sûr de vouloir approuver cet utilisateur ?')">
                                <div class="flex items-center justify-center space-x-2">
                                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                                    </svg>
                                    <span>Approuver</span>
                                </div>
                            </button>
                        </form>
                        
                        <form method="POST" action="{{ path('admin_reject_user', {'id': user.id}) }}" class="inline">
                            <button type="submit" 
                                    class="btn-danger text-white font-medium py-3 px-6 rounded-2xl transition-all duration-300 text-center transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-white/20 shadow-lg hover:shadow-xl w-full"
                                    onclick="return confirm('Êtes-vous sûr de vouloir rejeter cet utilisateur ? Cette action est irréversible.')">
                                <div class="flex items-center justify-center space-x-2">
                                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                                    </svg>
                                    <span>Rejeter</span>
                                </div>
                            </button>
                        </form>
                    {% else %}
                        <div class="text-center p-4 glass-input rounded-2xl">
                            <svg class="w-12 h-12 text-green-400 mx-auto mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                            </svg>
                            <p class="text-green-400 font-medium">Utilisateur approuvé</p>
                            <p class="text-gray-400 text-sm">Le {{ user.approvedAt|date('d/m/Y à H:i') }}</p>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>

        <!-- Detailed Information Grid -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
            
            <!-- Personal Information -->
            <div class="glass-card rounded-3xl p-6 animate-slide-in-left">
                <div class="flex items-center space-x-3 mb-6">
                    <div class="w-10 h-10 bg-gradient-to-r from-blue-500 to-blue-600 rounded-full flex items-center justify-center">
                        <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                        </svg>
                    </div>
                    <h2 class="text-xl font-bold text-white">Informations personnelles</h2>
                </div>
                
                <div class="space-y-6">
                    <div class="glass-input rounded-2xl p-4">
                        <div class="flex items-center space-x-3 mb-2">
                            <div class="w-6 h-6 bg-gradient-to-r from-indigo-500 to-indigo-600 rounded-full flex items-center justify-center">
                                <svg class="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                                </svg>
                            </div>
                            <h3 class="text-white font-semibold">Nom complet</h3>
                        </div>
                        <p class="text-gray-300 text-lg">{{ user.prenom }} {{ user.nom }}</p>
                    </div>
                    
                    <div class="glass-input rounded-2xl p-4">
                        <div class="flex items-center space-x-3 mb-2">
                            <div class="w-6 h-6 bg-gradient-to-r from-green-500 to-green-600 rounded-full flex items-center justify-center">
                                <svg class="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 12a4 4 0 10-8 0 4 4 0 008 0zm0 0v1.5a2.5 2.5 0 005 0V12a9 9 0 10-9 9m4.5-1.206a8.959 8.959 0 01-4.5 1.207"/>
                                </svg>
                            </div>
                            <h3 class="text-white font-semibold">Adresse e-mail</h3>
                        </div>
                        <p class="text-gray-300 text-lg">{{ user.email }}</p>
                        <div class="mt-2">
                            {% if user.isVerified %}
                                <span class="text-green-400 text-sm">✓ Adresse vérifiée</span>
                            {% else %}
                                <span class="text-orange-400 text-sm">⏳ En attente de vérification</span>
                            {% endif %}
                        </div>
                    </div>
                    
                    <div class="glass-input rounded-2xl p-4">
                        <div class="flex items-center space-x-3 mb-2">
                            <div class="w-6 h-6 bg-gradient-to-r from-purple-500 to-purple-600 rounded-full flex items-center justify-center">
                                <svg class="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>
                                </svg>
                            </div>
                            <h3 class="text-white font-semibold">Rôle</h3>
                        </div>
                        <p class="text-gray-300 text-lg">
                                {% if user.role == 'parent' %}
                                    Parent
                                {% elseif user.role == 'grand_parent' %}
                                    Grand-parent
                                {% elseif user.role == 'autre' %}
                                {% if user.roleAutre %}
                                    {{ user.roleAutre }}
                                {% else %}
                                    Autre
                                {% endif %}
                            {% endif %}
                        </p>
                    </div>
                </div>
            </div>
            
            <!-- Account Activity -->
            <div class="glass-card rounded-3xl p-6 animate-slide-in-right">
                <div class="flex items-center space-x-3 mb-6">
                    <div class="w-10 h-10 bg-gradient-to-r from-emerald-500 to-emerald-600 rounded-full flex items-center justify-center">
                        <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
                        </svg>
        </div>
                    <h2 class="text-xl font-bold text-white">Activité du compte</h2>
                </div>
                
                <div class="space-y-6">
                    <div class="glass-input rounded-2xl p-4">
                        <div class="flex items-center space-x-3 mb-2">
                            <div class="w-6 h-6 bg-gradient-to-r from-yellow-500 to-orange-500 rounded-full flex items-center justify-center">
                                <svg class="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"/>
                                </svg>
                            </div>
                            <h3 class="text-white font-semibold">Points AlloParents</h3>
                        </div>
                        <p class="text-gray-300 text-2xl font-bold animate-counter-up">{{ user.points }}</p>
                        <p class="text-gray-400 text-sm mt-1">Points accumulés sur la plateforme</p>
                    </div>
                    
                    <div class="glass-input rounded-2xl p-4">
                        <div class="flex items-center space-x-3 mb-2">
                            <div class="w-6 h-6 bg-gradient-to-r from-blue-500 to-blue-600 rounded-full flex items-center justify-center">
                                <svg class="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                                </svg>
                            </div>
                            <h3 class="text-white font-semibold">Date d'inscription</h3>
                        </div>
                        <p class="text-gray-300 text-lg">{{ user.createdAt|date('d/m/Y à H:i') }}</p>
                        <p class="text-gray-400 text-sm mt-1">
                            {% set daysDiff = date().diff(user.createdAt).days %}
                            {% if daysDiff == 0 %}
                                Inscrit aujourd'hui
                            {% elseif daysDiff == 1 %}
                                Inscrit il y a 1 jour
                            {% else %}
                                Inscrit il y a {{ daysDiff }} jours
                            {% endif %}
                        </p>
                    </div>

                    {% if user.approvedAt %}
                        <div class="glass-input rounded-2xl p-4">
                            <div class="flex items-center space-x-3 mb-2">
                                <div class="w-6 h-6 bg-gradient-to-r from-green-500 to-green-600 rounded-full flex items-center justify-center">
                                    <svg class="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                                    </svg>
                                </div>
                                <h3 class="text-white font-semibold">Date d'approbation</h3>
                            </div>
                            <p class="text-gray-300 text-lg">{{ user.approvedAt|date('d/m/Y à H:i') }}</p>
                            <p class="text-gray-400 text-sm mt-1">Compte activé par un administrateur</p>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>

        <!-- Back to List Button -->
        <div class="mt-8 text-center animate-fade-in">
            <a href="{{ path('admin_pending_users') }}" class="btn-secondary text-white font-medium py-3 px-8 rounded-2xl transition-all duration-300 text-center transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-white/20 shadow-lg hover:shadow-xl inline-flex items-center space-x-2">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
                </svg>
                <span>Retour à la liste</span>
            </a>
    </div>

    </main>

    <!-- JavaScript for animations -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Parallax effect for floating orbs
            document.addEventListener('mousemove', (e) => {
                const x = e.clientX / window.innerWidth;
                const y = e.clientY / window.innerHeight;
                
                const orbs = document.querySelector('.floating-orbs');
                if (orbs) {
                    orbs.style.transform = `translate(${x * 8}px, ${y * 8}px)`;
                }
            });

            // Animate counter
            function animateCounter() {
                const counter = document.querySelector('.animate-counter-up');
                if (counter) {
                    const target = parseInt(counter.textContent);
                    let current = 0;
                    const increment = target / 20;
                    const timer = setInterval(() => {
                        current += increment;
                        if (current >= target) {
                            counter.textContent = target;
                            clearInterval(timer);
                        } else {
                            counter.textContent = Math.floor(current);
                        }
                    }, 75);
                }
            }

            // Start counter animation after a delay
            setTimeout(animateCounter, 800);
        });
    </script>
</body>
</html>